# Eventra Frontend Environment Variables
# In unified fullstack deployment (where backend serves the frontend build), leave this empty or omit.
# In decoupled deployment (e.g. Frontend on Vercel/Netlify, Backend on Render/Railway), specify your backend API URL:
# VITE_API_URL=https://your-eventra-backend.onrender.com/api
